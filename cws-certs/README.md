Instructions for creating a cert.
------------------------------------

_Running this script will result in the replacement of existing keystore and truststore in `install/` by new certs._

####Execute script with command:

```
./generate-certs.sh
```

